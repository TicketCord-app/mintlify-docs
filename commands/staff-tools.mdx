---
title: "Staff Tools"
description: "Commands for staff productivity - notes, tags, transcripts, and quick replies"
---

These commands help staff work efficiently and maintain ticket organization.

## /note

Manage internal staff notes on a ticket. Notes are only visible to staff members.

```
/note add <content>
/note view
/note remove <number>
```

### Subcommands

| Subcommand | Description |
|------------|-------------|
| `add` | Add a new note to the ticket |
| `view` | View all notes on the ticket |
| `remove` | Remove a specific note |

**Permission:** Staff only

### Adding Notes

```
/note add content:Customer is a VIP - prioritize response
/note add content:Waiting for billing team confirmation
```

Notes include:
- The staff member who created it
- Timestamp when created
- The note content

### Viewing Notes

```
/note view
```

Displays all notes on the current ticket in chronological order.

### Removing Notes

```
/note remove note_id:1
```

<Info>
  Notes are internal and never visible to the ticket creator or other non-staff users.
</Info>

---

## /tag

Manage tags on the current ticket for organization and filtering.

```
/tag add <name>
/tag remove <name>
/tag list
/tag clear
```

### Subcommands

| Subcommand | Description |
|------------|-------------|
| `add` | Add a tag to the ticket |
| `remove` | Remove a tag from the ticket |
| `list` | List all tags on the ticket |
| `clear` | Remove all tags from the ticket |

**Permission:** Staff only

### Adding Tags

```
/tag add tag:billing
/tag add tag:urgent
/tag add tag:needs-followup
```

<Tip>
  Tags help you filter and search tickets in the dashboard. Use consistent naming for best results.
</Tip>

### Viewing Tags

```
/tag list
```

Shows all tags currently on the ticket.

### Removing Tags

```
/tag remove tag:urgent
/tag clear
```

---

## /transcript

Generate a transcript of the ticket conversation.

```
/transcript [channel] [user] [limit] [evidence]
```

| Option | Type | Required | Description |
|--------|------|----------|-------------|
| `channel` | Channel | No | Send the transcript to this channel instead of the configured one (staff only) |
| `user` | User | No | DM the transcript to a specific user (staff only) |
| `limit` | Integer | No | Only include the most recent N messages (default: all) |
| `evidence` | Boolean | No | Include Discord IDs and message links (staff only) |

**Permission:** Ticket creator or staff. The `channel`, `user`, and `evidence` options are staff only.

**What happens:**
- Creates a formatted record of all messages
- Includes timestamps, usernames, and message content
- Attachments and embeds are referenced
- Transcript is saved according to your settings

### Transcript Formats

The transcript is generated as an HTML file and delivered to:

| Destination | When |
|-------------|------|
| **Transcript channel** | By default, the channel configured in the dashboard |
| **Another channel** | When staff pass the `channel` option |
| **A user's DMs** | When staff pass the `user` option |

The command needs a transcript channel configured in the dashboard. Evidence transcripts (`evidence:True`) can only go to staff members or staff-only channels.

<Info>
  Configure transcript settings in your dashboard under **Ticket Lifecycle** → **Transcripts**.
</Info>

---

## /reply

Send a pre-configured canned response.

```
/reply <shortcut>
```

| Option | Type | Required | Description |
|--------|------|----------|-------------|
| `shortcut` | Text (autocomplete) | Yes | Shortcut of the canned response |

**Permission:** Staff only  
**Plan:** Basic or higher

**What happens:**
- Sends the pre-configured response message
- Can include variables like ticket number, user mention
- Saves time on common responses

### Setting Up Replies

1. Go to your dashboard
2. Navigate to **Quick Replies** (or **Canned Responses**)
3. Create responses with names and content
4. Use `/reply` to send them

**Example responses:**

| Name | Content |
|------|---------|
| `greeting` | "Thanks for contacting support! I'll be with you shortly." |
| `resolved` | "Glad we could help! Feel free to open a new ticket if you need anything else." |
| `escalate` | "I'm escalating this to our senior team. You'll hear back within 24 hours." |

<Tip>
  Use variables in your replies: `{{user}}` for the creator mention and `{{ticketNumber}}` for the ticket number. The older `{user}` and `{ticketNumber}` forms still work.
</Tip>

---

## /priority

Set the priority level for a ticket. Adds a visual indicator to the channel name.

```
/priority <level>
```

| Option | Type | Required | Description |
|--------|------|----------|-------------|
| `level` | Choice | Yes | Priority level to set |

**Choices:**
- `urgent` - Adds red indicator
- `high` - Adds orange indicator
- `medium` - Adds yellow indicator
- `low` - Adds green indicator

**Permission:** Staff only  
**Plan:** All plans

**What happens:**
- Channel name gets priority emoji prefix
- Ticket is flagged in the dashboard
- Helps staff identify critical issues

**Example:**
```
/priority level:urgent
```

Channel becomes: `🔴-ticket-0001`

<Warning>
  Channel renaming is rate-limited by Discord. Rapid priority changes may be delayed.
</Warning>

---

## /stats

View ticket statistics for the current server.

```
/stats [period]
```

| Option | Type | Required | Description |
|--------|------|----------|-------------|
| `period` | Choice | No | Time period for stats |

**Choices:**
- `7d` - Last 7 days
- `30d` - Last 30 days
- `90d` - Last 90 days
- `all` - All-time statistics

**Permission:** Staff only  
**Plan:** Basic or higher

**Statistics include:**
- Total, open, and closed tickets
- Average first response time and average resolution time
- Top ticket creators
- Priority breakdown
- Tickets created per day

**Example:**
```
/stats period:week
```

<Info>
  For detailed analytics, visit the **Analytics** section in your dashboard.
</Info>

<Card title="Administration Commands" icon="arrow-right" href="/commands/admin">
  Learn about setup, embeds, and approvals
</Card>

---

## /ai-help

Get an AI-generated reply suggestion for the current ticket. The suggestion is shown only to you; you can send it as is, edit it first, or discard it.

```
/ai-help
```

**Permission:** Staff only  
**Plan:** Pro or higher

---

## /summary

Generate an AI summary of the ticket conversation, for example before handing the ticket to a colleague.

```
/summary [format]
```

| Option | Type | Required | Description |
|--------|------|----------|-------------|
| `format` | Choice | No | Summary style |

**Choices:**
- `detailed` - Full analysis
- `executive` - Key points
- `handoff` - Staff transition notes
- `brief` - Quick overview

**Permission:** Staff only  
**Plan:** Pro or higher

---

## /context

Show how much of the AI conversation context this ticket is using.

```
/context
```

**Permission:** Staff only

---

## /translation

Turn AI translation on or off for the current ticket.

```
/translation <enabled>
```

| Option | Type | Required | Description |
|--------|------|----------|-------------|
| `enabled` | Boolean | Yes | `True` to enable translation, `False` to disable |

**Permission:** Staff only. Translation must be enabled for the server in the dashboard first.

---

## /claims

See the tickets you have claimed, or another staff member's claimed tickets.

```
/claims [user]
```

| Option | Type | Required | Description |
|--------|------|----------|-------------|
| `user` | User | No | Show another staff member's claimed tickets |

**Permission:** Staff only

---

## /feedback

Ask the ticket creator to rate the support they received.

```
/feedback <ticket> <rating> [staff] [comment]
```

| Option | Type | Required | Description |
|--------|------|----------|-------------|
| `ticket` | Text (autocomplete) | Yes | The ticket to rate |
| `rating` | Choice | Yes | 1 to 5 stars |
| `staff` | Text (autocomplete) | No | The staff member who helped |
| `comment` | Text | No | Optional comment |

---

## /modmail

Inspect ModMail evidence for the current ticket.

```
/modmail message <number>
/modmail evidence
```

| Subcommand | Description |
|------------|-------------|
| `message <number>` | Look up the linked Discord IDs for a message number from the ModMail transcript |
| `evidence` | Show a compact evidence pack for this ModMail ticket |

**Permission:** Staff only. Works in ModMail ticket channels only.
